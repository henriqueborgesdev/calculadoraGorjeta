package com.example.calculadoragorjeta.navigation

import kotlinx.serialization.Serializable

/**
 * Rotas de navegação type-safe (Navigation Compose 2.8+): cada destino é
 * representado por um objeto @Serializable, em vez de uma String de rota
 * escrita à mão — mesmo padrão do projeto de exemplos da aula.
 */

@Serializable
object Menu

@Serializable
object CalculadoraGorjeta
